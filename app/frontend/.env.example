# Copy to .env for local dev (optional — defaults to http://localhost:4000),
# or set VITE_BACKEND_URL in Vercel's project settings for production,
# pointing at your deployed Render backend URL.
VITE_BACKEND_URL=
